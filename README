Provides an equivalent of Javascript's string class. It also uses
mbstring functions by default if you have mbstring enabled.

See examples.php for extensive examples.

Require PHP 5.2+, but PHP 5.3 is preferred.